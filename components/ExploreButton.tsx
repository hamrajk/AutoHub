const ExploreButton = ({}) => {
  return (
    <a
      className={`custom-btn bg-primary-blue text-white rounded-full mt-10 max-w-[180px]`}
      href="#about"
    >
      Explore Cars
    </a>
  );
};

export default ExploreButton;
